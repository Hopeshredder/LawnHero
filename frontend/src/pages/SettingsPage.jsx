import { Link } from "react-router-dom";

export default function Settings() {
  return (
    <div className="px-4 sm:px-8 md:px-16 lg:px-24">
      <h1>SETTINGS PAGE
        Pull profile info
        have a form where you can update info and send it to backend to persist
      </h1>
    </div>
  );
}
